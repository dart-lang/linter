name: "issue labeler"

on:
  issues:
    types: [opened, edited]

permissions: read-all

jobs:
  triage:
    permissions:
      contents: read
      issues: write
    runs-on: ubuntu-latest
    steps:
      - uses: github/issue-labeler@6bea9ed491bb74fce38775b3d863e64a59dbd9eb
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          configuration-path: .github/issue-labeler.yml
          enable-versioned-regex: 0
          include-title: 1
