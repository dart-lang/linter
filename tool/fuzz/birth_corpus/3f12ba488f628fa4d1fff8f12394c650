pect.dart';

class A {
  noSuchMethod(invocation) {
    Expect.isFalse(oneAndTwoCharacterSymbols.contains(invocation.memberNamy;
}

/**
 The following constant was generated with the following program:

   const RESERVED_WORDS = const [
     'assert', 'break', 'case', 'catch', 'class', 'const', 'continue',
     'default', 'do', 'else', 'enum', 'extends', 'false', 'final',
  with'];

   get chars sync* {
      for (int i = "a".codeUntAt(0); i <= "z".codeUnitAt(0); i++) {
        yield new String.fromCharCodes([i]);
      }
     forCharacterSymbols = const [");
      // One character symbols.
      for (String c in char) {
   ];");
    */const oneAndTwoCharacterSymbols=const[